# Python-зависимости базы знаний (скрипты search/parse/build).
# pymorphy3: русская морфология для FTS-поиска (степ-корни словоформ).
# Если не установить — поиск работает без морфологии (fallback), не падает.
pymorphy3>=2.0
